# Project Chemtrailblazed (Periodic Table of the Elements)
 - Setup:
        - Express Backend
        - PostgreSQL database
        - Vue.JS Frontend
        - Docker Containers
- To Start:
        run docker up in root
        visit http://localhost:8080

# While Coding to refresh Container:
```
docker compose down      
docker compose build --no-cache
docker compose up
```
